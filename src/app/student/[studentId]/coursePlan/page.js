import {CreateCoursePlan} from '@/components/coursePlan';
import {Box} from '@mui/material';

export default function CoursePlanPage() {
    return (
        <Box mt={2}>
            <CreateCoursePlan />
        </Box>
    );
}
