import {ViewAllAlerts} from '@/components/alertsAndNudges';
import {Box} from '@mui/material';

export default function AlertsPage() {
    return (
        <Box mt={2}>
            <ViewAllAlerts />
        </Box>
    );
}
