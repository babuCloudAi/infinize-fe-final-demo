import {Box, Typography} from '@mui/material';

export default function Student() {
    return (
        <Box component="main">
            <Typography variant="h4">Student</Typography>
            <Typography>Here is your Student page content!</Typography>
        </Box>
    );
}
