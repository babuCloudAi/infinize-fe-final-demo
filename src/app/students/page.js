import {Box, Typography} from '@mui/material';

export default function Students() {
    return (
        <Box component="main">
            <Typography variant="h4">My Students</Typography>
            <Typography>Check the list of your students</Typography>
        </Box>
    );
}
