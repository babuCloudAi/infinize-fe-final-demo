import {Box, Typography} from '@mui/material';

export default function DataSources() {
    return (
        <Box>
            <Typography variant="h4">DataSources</Typography>
            <Typography>Here is your DataSources page content!</Typography>
        </Box>
    );
}
