import {Box, Typography} from '@mui/material';

export default function Settings() {
    return (
        <Box>
            <Typography variant="h4">Settings</Typography>
            <Typography>Here is your Settings page content!</Typography>
        </Box>
    );
}
